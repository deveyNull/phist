#include <stdio.h>
#include <stdlib.h>

int main()
{

	int items, i;
	int *array;

	printf("How many items?");
	scanf(%d, &items);
	
	array = (int*) malloc( items * sizeof(int) ;
	
	for (i=0, i<items, i++):
	{	
		printf("Item %d?", i);
		scanf(%d, arrray[i]);
		
	}
}
